Status: active
Created: 2026-06-15
Updated: 2026-06-15

# Project Name

The project is named **Open Streaming Lab**.

Canonical machine/package naming:

- Repository/remote name: `open-streaming-lab`
- Desired folder name when safe to rename outside the active tool session: `open-streaming-lab`
- Python package distribution name: `open-streaming-lab`
- Node helper package name: `open-streaming-lab-mockingbird-tools`

The current working directory may still be named `streaming-tutorial` until it is renamed from its parent directory. The Python import package remains `streaming_tutorial` for now to avoid a broad source rename during naming cleanup. A future refactor may rename the import package if desired.
