# Python Kafka + Mockingbird Tutorial Scaffold

ID: ticket:20260613-python-kafka-mockingbird-tutorial
Type: Ticket
Status: done
Created: 2026-06-13
Updated: 2026-06-13
Risk: medium - new scaffold spans Docker Compose, Apache Kafka networking, a Node Mockingbird bridge, and Python Kafka producer/consumer code, but it is isolated to a new tutorial folder and reversible.
Priority: high - this is the operator-approved next step after research.

## Summary

Create a beginner-friendly local Kafka tutorial scaffold in this empty `streaming-tutorial` folder. The bounded result is a Python-centered tutorial where Docker Compose runs local Apache Kafka and Kafbat UI, Mockingbird remains the fake event source through a small Node JSONL bridge, and Python scripts create topics, produce Mockingbird events to Kafka, and consume them while explaining practical Kafka concepts.

Single closure claim: this ticket is complete when the repository contains a runnable Python Kafka tutorial scaffold, documented commands, and validation evidence proportional to a local-learning setup.

## Related Records

- `research:20260613-local-kafka-mockingbird-tutorial` - records the source-backed findings, open-source-first constraints, and tradeoffs behind using local Apache Kafka and Mockingbird rather than managed Confluent Cloud/Tinybird ingestion.
- `/Users/crlough/Code/personal/CLAUDE.md` - project-wide open-source-first, TypeScript strict-by-default, and research-first constraints; Python work should use `uv`.

## Scope

May create and edit tutorial files under this repository root, including:

- `README.md`, `.gitignore`, `compose.yaml`, and optional `Makefile`.
- Python packaging/configuration files such as `pyproject.toml` and optionally `uv.lock` if generated by validation.
- Python source under `src/streaming_tutorial/` for config, admin/topic creation, producer, and consumer code.
- A small Node helper under `tools/` plus `package.json`/lockfile if needed to use `@tinybirdco/mockingbird` as the event generator.
- This ticket's Current State and Journal while executing.

Must preserve these constraints:

- Keep the stack local and open-source/self-hosted: Apache Kafka, Kafbat UI, Mockingbird, Python Kafka client. Do not introduce managed Kafka, Confluent Cloud, Tinybird SaaS, Firebase, or other proprietary/managed services.
- Keep the first tutorial focused on Kafka basics: topics, producers, consumers, offsets, consumer groups, partitions, local UI, and troubleshooting.
- Do not add schema registry, Kafka Connect, Flink/Spark, databases, dashboards, or multi-broker production topology in this ticket. Mention them only as future chapters if useful.
- Do not persist secrets or credentials in files, records, examples, or evidence.

Read scope for the implementation run:

- This ticket and related research record.
- Existing repository root (currently empty aside from Loom records).
- External source facts already summarized in the research record; recheck only if implementation reveals a conflict.

Write scope for the implementation run:

- Files named above in this repository.
- This ticket journal/current state.
- Evidence record(s) only if validation outputs should be preserved for closure.

Stop conditions:

- Stop and ask the operator if using Mockingbird from Python would require abandoning Mockingbird as the event source.
- Stop if the tutorial would need cloud credentials or managed services.
- Stop if a dependency license or service model appears to conflict with open-source-first constraints.
- Stop if validation requires destructive Docker cleanup beyond this tutorial's containers/volumes.

Verification posture: observation-first. Prefer static validation and command checks (`docker compose config`, Python compile/checks, Node syntax checks, optional dependency lock/install if safe). Full Kafka runtime smoke test is useful but not mandatory if Docker or dependency availability blocks it; record limits honestly.

## Acceptance

- ACC-001: A local Kafka Compose stack exists and uses Apache Kafka KRaft plus Kafbat UI with host-accessible Kafka and UI ports.
  - Evidence: `docker compose config` succeeds or the reason it was not run is recorded; file inspection confirms no managed/cloud Kafka dependency.
  - Audit: Review listener config for host and Docker network correctness and open-source-only services.

- ACC-002: The Python project is runnable with `uv` and includes scripts for topic/admin setup, producing, and consuming Kafka messages.
  - Evidence: Python source compiles and README shows `uv` commands; dependency and script definitions exist in `pyproject.toml`.
  - Audit: Review that scripts have clear defaults, friendly errors, and no secrets.

- ACC-003: Mockingbird is the fake event source for the producer path, even though the Kafka client is Python.
  - Evidence: A Node helper uses `@tinybirdco/mockingbird` to emit JSONL, and the Python producer reads that stream and publishes records to Kafka with configurable topic/template/EPS/limit.
  - Audit: Review that the bridge is understandable for a learner and does not silently replace Mockingbird with unrelated fake data.

- ACC-004: The README teaches the hands-on flow and the core Kafka concepts the operator wants for job-search learning.
  - Evidence: README includes prerequisites, install/start commands, produce/consume workflow, UI instructions, troubleshooting, cleanup, and explanations of topics/producers/consumers/consumer groups/offsets/partitions.
  - Audit: Review the tutorial as a beginner: commands should be copy/pasteable and sequence should not depend on unstated chat context.

- ACC-005: Validation results and limitations are recorded before claiming completion.
  - Evidence: Commands run and outcomes are summarized in the ticket and/or evidence record.
  - Audit: If full Kafka smoke testing is skipped, closure must explicitly state what was and was not verified.

## Current State

Implementation scaffold is complete and accepted as the baseline for next-level work. Validation evidence is recorded in `evidence:20260613-python-kafka-tutorial-validation`. Static validation passed (`make test`, `uv run pytest`, `node --check`, `docker-compose config`, Mockingbird sample generation, entrypoint help checks). Operator-requested Colima/Docker debugging found and fixed two issues: Colima default profile was stopped in this shell, and `~/.docker/config.json` still referenced Docker Desktop's missing credential helper. Runtime validation then found and fixed a Kafka data-volume permission issue and a noisy `localhost`/IPv6 listener issue. Full local Kafka smoke testing now passes: Kafka and Kafbat UI start under Colima, topic creation succeeds, 5 Mockingbird events are produced, and 5 events are consumed from Kafka. Ralph audit review was attempted with `reviewer`, `delegate`, and `oracle` subagents, but each timed out before returning usable findings, so no `Type: Audit` record was created. Status is `done`. The missing independent audit is accepted as residual risk for this local tutorial scaffold because full runtime smoke testing passed and the follow-on roadmap now starts from this baseline.

## Journal

- 2026-06-13: Created ticket with Status `open` after operator selected the Python Kafka scaffold. First move is to set Status `active`, implement the scaffold, then validate and record evidence/limits.
- 2026-06-13: Set Status to `active` for the current-session Ralph implementation run.
- 2026-06-13: Created tutorial scaffold files: README, Compose stack, Makefile, Python package/scripts/tests, Mockingbird Node JSONL helper, and npm/uv lockfiles.
- 2026-06-13: Ran validation: `python3 -m compileall src`, `node --check tools/mockingbird-jsonl.mjs`, `docker-compose config`, `npm install`, `npm run mockingbird:templates`, `npm run mockingbird:sample`, `uv sync --extra dev`, `uv run pytest`, entrypoint help checks, and `make test`. Recorded results and limitations in `evidence:20260613-python-kafka-tutorial-validation`.
- 2026-06-13: Attempted Ralph audit review via `reviewer` (300s), `reviewer` focused retry (180s), `delegate` (120s), and `oracle` (120s); all timed out without findings. Set Status to `review` rather than `closed` because audit did not complete and Docker runtime smoke remains unverified.
- 2026-06-13: Reopened to `active` for operator-requested Colima/Docker context debugging so the Kafka runtime smoke test can potentially run.
- 2026-06-13: Debugged Docker/Colima. `colima ls` showed the default profile was stopped in this shell; `colima start` restored Docker Engine connectivity. `docker-compose up -d` then failed because `~/.docker/config.json` referenced missing `docker-credential-desktop`; backed up the config and removed the stale `credsStore` entry.
- 2026-06-13: Runtime validation exposed Kafka startup failure from named volume permissions at `/tmp/kraft-combined-logs`; removed the volume mount from `compose.yaml`. First live smoke test worked but emitted noisy IPv6 `::1` warnings, so changed advertised host and Python default bootstrap server to `127.0.0.1:9092` and documented the Colima troubleshooting path in `README.md`.
- 2026-06-13: Reran full smoke successfully: `docker-compose down -v`, `docker-compose up -d`, `uv run kafka-admin create-topic --topic mockingbird.events --partitions 3`, `uv run kafka-produce --topic mockingbird.events --template "Web Analytics Starter Kit" --eps 5 --limit 5`, `uv run kafka-consume --topic mockingbird.events --group-id smoke-test-$(date +%s) --from-beginning --max-messages 5`, and `make test` all succeeded. Set Status back to `review` pending audit/closure decision.

- 2026-06-15: Accepted the validated scaffold as the baseline despite timed-out audit attempts; recorded closure/source-control evidence in `.loom/evidence/2026-06-15-baseline-closure-repo-hygiene.md` and moved this ticket to done.
